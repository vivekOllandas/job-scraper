{
  "name": "job-scraper",
  "version": "1.0.0",
  "description": "Scrapes Indeed and Naukri job listings and exposes them via API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npx playwright install --with-deps chromium"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
